# Bakery

## Unity UGUI 기반 캐주얼 베이커리 RPG  프로토타입

빵을 만들고 진열하는 운영 화면과, 몬스터를 처치하며 성장하는 전투 스테이지를 구현한 모바일 게임 프로토타입입니다.

현재 혼자 개발하고 있으며, 운영·전투 기능 구현부터 UI 구성, 데이터와 리소스 연결까지 담당하고 있습니다.
AI는 이미지와 테이블 데이터 제작, 코드 수정 및 검증을 보조하는 도구로 활용했습니다.

초기에는 더미 UI와 단색 캐릭터로 기능을 구성했습니다.
이후 캐릭터와 배경을 교체하면서 전투 애니메이션, 공격 방향 처리, 플레이어 추적 화면을 추가했습니다.

### 개발 정보

- 개발 형태 : 개인 개발
- 개발 상태 : 개발 진행 중
- 대상 플랫폼 : 모바일
- 주요 작업 : 운영·전투 기능 구현, UGUI 화면 및 팝업 구성, 데이터·리소스 연동
- 개발 환경 : Unity6, C#, UGUI, Animator/AnimationClip
- 사용 라이브러리 : SuperScroll
- 개발 보조 도구 : Claude, AI 이미지 생성 도구, Unity CLI

### 플레이 영상

## 빵 제조 화면 영상

https://github.com/user-attachments/assets/5207de49-0f1f-4878-8184-338381aecac7

빵 목록을 확인하고 제조하는 화면입니다.
스크롤 목록에는 SuperScroll을 사용하고, 빵 제조에 사용하는 테이블 데이터와 아이콘을 연결했습니다.

## 전투 스테이지

https://github.com/user-attachments/assets/fe0d79b4-231d-4ce4-8325-5848184757d3

몬스터와 전투하며 성장하는 스테이지입니다.
몬스터 생성에는 오브젝트 풀링을, 이동 경로 탐색에는 A* 알고리즘을 적용했습니다.
전투 중에는 플레이어 위치에 따라 전투 영역이 이동하고 HUD는 고정됩니다.

### 주요 구현

## 1. 상태머신을 통한 전투 흐름 관리

CharacterStateMachine 에서 전투 시작, 플레이어와 몬스터 생성, 몬스터 리스폰, 스테이지 클리어와 결과 팝업 호출을 처리합니다.

캐릭터의 전투 상태와 애니메이션을 연결하는 부분은 CharacterAnimationBridge로 분리했습니다.
Idle, Attack, Hit 애니메이션 호출을 이 컴포넌트에 모아, 캐릭터 로직에서 Animator를 직접 다루는 부분을 줄였습니다.

몬스터는 오브젝트 풀을 통해 생성·재사용하도록 구성했으며, 이동 경로 탐색에는 A* 알고리즘을 사용했습니다.

## 2. 이동과 공격이 겹칠 때의 방향 처리

이동 방향만으로 캐릭터의 좌우 방향을 결정하면, 이동하면서 공격할 때 공격 대상의 반대쪽을 바라보는 문제가 있었습니다.

이를 해결하기 위해 CharacterFacing 에서 상태에 따라 방향 결정 기준을 나눴습니다.

- 일반 이동 중에는 이동 방향을 기준으로 좌우 방향을 변경합니다.
- 공격 중에는 이동 방향보다 공격 대상의 방향을 우선합니다.

이렇게 처리해 이동 중에도 캐릭터가 공격 대상을 바라보도록 수정했습니다.
방향 전환은 CharacterFacing, 애니메이션 호출은 CharacterAnimationBridge 에서 담당하도록 역할을 나눴습니다.

## 3. 전투 영역과 고정 UI 분리

기존에는 배경, 캐릭터, HUD가 같은 Canvas 구조에 있어 플레이어를 따라 전투 영역만 이동시키기 어려웠습니다.

UGUI 기반의 전투 구조 안에서 배경과 캐릭터를 BattleWorld 아래로 분리하고, 타이머·수집 패널·버튼은 고정 UI로 유지했습니다.
캐릭터도 pageStage.CharacterRoot 를 통해 전투 영역 아래에 생성되도록 변경했습니다.

StageCameraFollow 는 플레이어 위치를 기준으로 BattleWorld 를 부드럽게 이동시킵니다.
실제 Camera 대신 UGUI의 전투 영역을 이동시켜, HUD 위치를 유지하면서 플레이어를 추적하도록 구현했습니다.

## 4. 운영 화면과 팝업 구성

Main, WorldMap, Stage 화면과 제조·진열·시설·레벨업·결과 팝업을 구성했습니다.

빵 제조 목록에는 SuperScroll을 사용했습니다.
UI를 정리하는 과정에서는 ScrollView와 GridLayout의 콘텐츠 위치가 어긋나는 문제를 수정하고, 버튼·패널·텍스트·보상 슬롯의 스타일을 맞췄습니다.

기존 UGUI 프리팹 구조를 유지하면서 운영 화면과 전투 화면에 목재·양피지 톤을 적용했습니다.

## 5. 캐릭터 리소스와 애니메이션 연결

초기의 단색 Image를 제빵사 캐릭터와 꿀 반죽 몬스터 이미지로 교체하고, 전투용 꿀숲 배경을 적용했습니다.

생성 이미지는 Sprite로 임포트해 Player와 Monster 프리팹에 연결했습니다.
이후 AnimatorController와 Idle, Attack, Hit 클립을 구성하고, 전투 상태에 따라 애니메이션이 재생되도록 연결했습니다.

이미지 교체에 필요한 임포트 설정과 프리팹 연결뿐 아니라, 상태에 따른 애니메이션과 좌우 방향 처리까지 함께 적용했습니다.

### AI 활용 

개인 개발 과정에서 필요한 아트 리소스와 데이터 초안을 만들고, 구현을 보조하는 데 AI를 활용했습니다.

| 활용 영역 | 작업 내용 |
| --- | --- |
| 캐릭터·배경 | 제빵사, 꿀 반죽 몬스터, 꿀숲 배경 이미지 제작 |
| 빵 아이콘 | Claude로 프롬프트를 정리한 뒤 나노바나나로 제작 |
| 스킬 이미지 | 레벨업 시 획득하는 스킬 이미지 제작 |
| 테이블 데이터 | Claude를 활용해 빵 제조용 테이블 시트 작성 |
| 개발 보조 | UI 구조 개선, 리소스 연결, 코드 수정 및 Unity CLI를 통한 검증 과정에 활용 |

생성 결과물을 프로젝트에 적용하는 과정에서는 Sprite 설정, 프리팹 참조, Animator 연결, UGUI 배치 등을 조정했습니다.
기존 게임 로직 안에서 리소스가 동작하도록 통합하는 작업도 함께 진행했습니다.

### 개선 결과

| 항목 | 초기 상태 | 개선 후 |
| --- | --- | --- |
| UI | 더미 배치와 기본 스타일 | 목재·양피지 톤으로 화면과 팝업 통일 |
| 캐릭터·몬스터 | 단색 Image | 제빵사·꿀 반죽 몬스터 리소스 적용 |
| 전투 배경 | 빵집 배경 재사용 | 전투용 꿀숲 배경 적용 |
| 애니메이션 | 없음 | Idle, Attack, Hit 연결 |
| 방향 처리 | 이동 방향 기준 | 공격 중 대상 방향 우선 |
| 화면 이동 | 고정된 전투 화면 | HUD를 유지하며 전투 영역이 플레이어를 추적 |

## 코드 하이라이트

전투 흐름, 레벨업 규칙, 오브젝트 재사용, 화면 추적을 중심으로 주요 코드를 정리했습니다.
첨부 파일은 프로젝트에서 발췌한 코드이며, 실행에는 관련 데이터와 다른 프로젝트 코드가 필요합니다.

### 1. 데이터 기반 몬스터 웨이브 관리

## 관련 코드 : [StageManager.cs](./CodeHighlights/StageManager.cs), [pageStage.cs](./CodeHighlights/pageStage.cs)

스테이지별 몬스터 등장 구성을 데이터로 관리하도록 구현했습니다.
웨이브 데이터에서 생성 시작·종료 시점, 생성 간격, 최대 생존 수를 읽고, 플레이 중에는 경과 시간과 현재 생존 수를 기준으로 몬스터를 생성합니다.

생성 수는 웨이블별 제한과 스테이지 전체 제한을 함께 확인합니다.
몬스터가 처치되면 활성 목록과 소속 웨이브의 생존 수를 갱신한 뒤 오브젝트 풀로 반환합니다.
활성 목록에서 제거된 몬스터만 처치 수에 반영하도록 처리했습니다.

남은 시간과 처치 수는 StageManager 에서 이벤트로 전달하고, pageStage 에서 HUD를 갱신합니다.
바인딩 대상이 변경되거나 페이지가 닫힐 때는 기존 이벤트 구독을 해제합니다.

## 주요 메서드

- LoadStageRuntimeData : 테이블 데이터를 플레이 중 사용할 웨이브 상태로 구성
- SpawnWaveMonsters : 생성 시간과 생존 수 제한 확인
- HandleMonsterDefeated : 활성 목록 정리, 처치 수 갱신, 풀 반환
- BindStageManager / UnbindStageManager : HUD 이벤트 연결과 해제

### 풀링된 몬스터와 이전 투사체의 타겟 구분

## 관련 코드 : [CharacterBase.cs](./CodeHighlights/CharacterBase.cs), [Projectile.cs](./CodeHighlights/Projectile.cs)

풀에서 재사용하는 몬스터는 반환 전후에 같은 오브젝트 참조를 유지합니다.
따라서 투사체가 타겟의 존재 여부와 활성 상태만 확인하면, 재사용된 몬스터를 이전 타겟으로 인식할 수 있습니다.

이를 구분하기 위해 캐릭터를 초기화 할 때마다 증가하는 LifeGeneration 값을 사용했습니다.
투사체는 발사 시점의 타겟과 세대 값을 함께 저장합니다.

추적 중 타겟이 사망하거나 비활성화된 경우 뿐 아니라, 저장한 세대 값과 현재 값이 달라진 경우에도 투사체를 제거합니다.
같은 오브젝트가 다시 사용되더라도 이전 공격이 계속 추적하지 않도록 처리했습니다.

## 주요 메서드

- CharacterBase.ResetCharacter : 체력·타겟·공격 시간을 초기화하고 세대 값 증가
- Projectile.Initialize : 발사 시점의 타겟과 세대 값
- Projectile.Update : 타겟 상태와 세대 값 확인 후 이동·피해 처리

### 3. 레벨업 선택 검증과 공격 방식 분리

## 관련 코드 : [PlayerLoadout.cs](./CodeHighlights/PlayerLoadout.cs), [WeaponStrategies.cs](./CodeHighlights/WeaponStrategies.cs)

무기·패시브의 테이블 정의와 현재 플레이에서 획득한 레벨을 분리했습니다.
레벨업 후보를 만들 때는 해금 챕터, 최대 레벨, 보유 슬롯 수를 확인합니다.
무기는 지원하는 공격 방식인지, 다음 레벨 데이터가 존재하는지도 검사합니다.

선택을 적용하는 TryApply 에서도 현재 후보 목록을 기준으로 ID·종류·레벨을 다시 확인합니다.
이미 적용되어 레벨이 달라진 선택이나 현재 조건에서 사용할 수 없는 선택은 거부합니다.

공격 방식은 IWeaponStrategy 구현으로 나누고, 실행 간격은  WeaponRuntime 에서 관리합니다.
PlayerLoadout 은 무기 데이터와 강화·패시브 효과를 조합해 공격 수치를 계산하고, 각 공격 구현에 전달합니다.

## 주요 메서드와 타입

- GetAvaliableChoices : 현재 조건에 맞는 무기·패시브 후보 구성
- TryApply : 선택 유효성 재확인과 보유 레벨 갱신
- GetWeaponStats : 기본 수치, 강화 배율, 패시브 효과를 조합한 무기 수치 계산
- IWeaponStrategy : 공격 방식별 실행 인터페이스
- WeaponRuntime.Tick : 공격 실행과 쿨다운 관리

### 4. 고정 HUD를 유지하는 UGUI 전투 화면 추적

## 관련 코드 : [StageCameraFollow.cs](./CodeHighlights/StageCameraFollow.cs), [pageStage.cs](./CodeHighlights/pageStage.cs)

UGUI 기반 전투 화면에서 플레이어를 따라 화면이 이동하도록 구현했습니다.
배경과 캐릭터는 BattleWorld 아래에 배치하고, 타이머·체력·경험치 등의 HUD는 고정 영역에 유지했습니다.

StageCameraFollow 는 플레이어 위치를 기준으로 전투 영역의 목표 위치를 계산합니다.
플레이어를 처음 연결 할 때는 위치를 즉시 맞추고, 이후에는 LateUpdate 에서 시간 간격을 반영한 지수 감쇠 보간으로 이동합니다.

목표 위치는 월드 크기와 표시 영역 크기를 기준으로 제한합니다.
실제 Camera를 이동시키지 않고 RectTransform.anchoredPosition 을 조정해 기준 UGUI 구조 안에서 추적 화면을 구성했습니다.

## 주요 메서드

- Bind : 추적할 플레이어 연결
- SnapToTarget : 최초 연결 시 위치 맞춤
- Follow : 시간 간격을 반영한 부드러운 이동
- GetClampedCameraOffset : 월드와 표시 영역 크기에 따른 목표 이동 범위 제한








